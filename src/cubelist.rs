use super::cube::Cube;

pub struct CubeList(Vec<Cube>);

impl CubeList {
    pub fn len(&self) -> usize {
        unimplemented!();
    }

    pub fn contains(&self, cube_x: &Cube) -> bool {
        unimplemented!();
    }
}
